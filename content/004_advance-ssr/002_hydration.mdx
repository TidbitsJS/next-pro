---
title: "Hydration"
abstract: "Learn how React.js handles hydration."
---

# Hydration

React.js uses a virtual DOM to render the UI. This means that the DOM is not updated immediately when a component's state changes. Instead, React.js creates a virtual DOM tree, compares it to the previous virtual DOM tree, and then updates the real DOM with the differences.
